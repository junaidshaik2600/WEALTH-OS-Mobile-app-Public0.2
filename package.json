{
  "name": "wealth-os",
  "version": "5.0.0",
  "description": "WEALTH OS - Complete Finance App - 100% Offline",
  "scripts": {
    "build": "npx cap copy android && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}